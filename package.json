{
  "name": "neon-slice",
  "version": "1.0.0",
  "private": true,
  "description": "Neon Slice — 100 level reflex game, packaged for Android via Capacitor",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/admob": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
